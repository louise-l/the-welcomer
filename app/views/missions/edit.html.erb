<div class="container">
  <%= simple_form_for [@user, @mission], remote: true do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :status %>
    <%= f.button :submit, class: 'button' %>
  <% end %>
</div>
